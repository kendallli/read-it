
An iOS app that can read RSS feeds

Selectable RSS links

CoreData is used to save the titles and descriptions of feed items.

Default Image is used in cause there is no image pulled from server.

Xcode Version 8.0 (8A218a)
iOS Development Target : 10.0

Podfile

platform :ios, '7.0'
target "Read It" do
pod 'SVProgressHUD'
pod 'MWFeedParser'
pod 'KINWebBrowser'
pod 'AFNetworking'
end

before running the project
run 
pod install
